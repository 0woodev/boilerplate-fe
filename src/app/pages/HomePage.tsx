import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'

export function HomePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Boilerplate ready</CardTitle>
        <CardDescription>
          앱 셸이 준비되었습니다. 여기에 첫 화면을 추가하세요.
        </CardDescription>
      </CardHeader>
      <CardContent>
        {/* TODO: replace with your first screen */}
      </CardContent>
    </Card>
  )
}
