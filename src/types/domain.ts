/**
 * 도메인 타입 정의 — BE API 응답 shape 과 1:1 대응.
 *
 * 새 도메인 추가 시 여기에 타입 정의 후 hooks/ 에서 사용.
 */

// 예시
// export interface User {
//   user_id: string
//   email: string
//   name: string
//   created_at: string
// }
