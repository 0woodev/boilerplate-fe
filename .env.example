# 로컬 개발용 — `cp .env.example .env.local` 후 수정
# CI/CD 빌드 시 GitHub Actions 환경변수가 자동 주입되므로
# 이 파일은 로컬 개발에서만 사용됨.

VITE_PROJECT_NAME=boilerplate-app
VITE_BE_URL=http://localhost:5001

# MSW(Mock Service Worker) 모킹 모드 on/off.
# 미설정 시 DEV 모드면 자동 ON. 실제 BE 연동 시 false.
VITE_USE_MSW=true
